# `ferrograd-nn`

Standard operations for use in `fused_gpu` and `ferrograd`.

Known correct operations:

- SGD
- Matrix multiplication
- Addition
- Subtraction
- Multiplication
- Division

Other operations are likely trivially correct but not tested (e.g. `tanh`, MSE loss).
